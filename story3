Create database and security authentication

this is the clone copy of the netbanking repository.
need to practice on clone copy and fork and passwordless.
need to practice git reset --soft
git reset --mixed
git reset --hard
